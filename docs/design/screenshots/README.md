# Prototype screenshots

Run `cd frontend && pnpm capture:design` to regenerate the approved mobile and desktop records for
the six fixture-backed milestone surfaces. The default end-to-end suite excludes these visual-only
captures so routine CI asserts behavior without turning every pixel into a release gate.
